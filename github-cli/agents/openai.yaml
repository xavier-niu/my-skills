interface:
  display_name: "GitHub CLI"
  short_description: "Use gh for GitHub services, not Git operations"
  default_prompt: "Use $github-cli for this GitHub service task while keeping native Git operations on git."
